<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.myjinxiaocunclaude.mapper.SaleOrderMapper">

    <resultMap id="SaleOrderVoResultMap" type="org.example.myjinxiaocunclaude.vo.SaleOrderVO">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="customerId" column="customer_id"/>
        <result property="customerName" column="customer_name"/>
        <result property="userId" column="user_id"/>
        <result property="userName" column="username"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <!-- 嵌套查询: 用主表 id 查询订单明细列表 -->
        <collection property="items" column="id" select="selectItemByOrderId"/>
    </resultMap>

    <select id="findPage" resultMap="SaleOrderVoResultMap">
        SELECT o.*, c.name AS customer_name, u.username
        FROM sale_order o
        LEFT JOIN customer c ON o.customer_id = c.id
        LEFT JOIN sys_user u ON o.user_id = u.id
        WHERE o.is_deleted = 0
        <if test="orderNo != null and orderNo != ''">
            AND o.order_no LIKE CONCAT('%', #{orderNo}, '%')
        </if>
        ORDER BY o.create_time DESC
    </select>

    <select id="selectItemByOrderId" resultType="SaleOrderItem">
        SELECT * FROM sale_order_item WHERE order_id = #{orderId} AND is_deleted = 0
    </select>

    <select id="selectTodayOrders" resultMap="SaleOrderVoResultMap">
        SELECT o.*, c.name AS customer_name, u.username
        FROM sale_order o
        LEFT JOIN customer c ON o.customer_id = c.id
        LEFT JOIN sys_user u ON o.user_id = u.id
        WHERE o.is_deleted = 0
          AND o.create_time BETWEEN #{start} AND #{end}
        ORDER BY o.create_time DESC
    </select>

</mapper>
